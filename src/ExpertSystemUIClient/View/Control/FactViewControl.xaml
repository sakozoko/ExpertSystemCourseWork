<UserControl x:Class="ExpertSystemUI.View.Control.FactViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="0 0 10 0">
            <TextBlock Text="{Binding Name}" Style="{StaticResource MaterialDesignTextBlock}"/>
            <TextBlock Text="Name" Style="{StaticResource MaterialDesignHelperTextBlock}"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="10 0 15 0">
            <TextBlock Text="{Binding Condition}" Style="{StaticResource MaterialDesignTextBlock}"/>
            <TextBlock Text="Condition" Style="{StaticResource MaterialDesignHelperTextBlock}"/>
        </StackPanel>
        <StackPanel Grid.Column="2" Margin="10 0 0 0">
            <TextBlock Text="{Binding InputValue}" Style="{StaticResource MaterialDesignTextBlock}"/>
            <TextBlock Text="InputValue" Style="{StaticResource MaterialDesignHelperTextBlock}"/>
        </StackPanel>
    </Grid>
</UserControl>
